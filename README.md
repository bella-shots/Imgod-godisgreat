# Master Company Portal

This repository is controlled using the architecture-first, spec-driven AI development workflow.

## Current architecture
Google Sites + Google Sheets + Google Forms + Google Drive + Google Apps Script.

## AI control
Read `agents.md` first.

Then read the six files under `context/`, followed by the requested feature specification.

## Development sequence
1. Google Drive structure
2. Master Google Site
3. Google Sheets + Forms
4. Apps Script automation
5. Testing + permissions + handover

## Repository history
`Phase-1` through `Phase-5` contain the detailed phase documentation, source-derived requirements and working prompt/report/feedback areas.

The previous React/Vite implementation is intentionally not part of the current architecture.
