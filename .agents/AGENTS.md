# Master Company Repository Agent Entry

## FIRST ACTION — STOP

This repository is a controlled specification-first project.

**Do not initialize, scaffold, generate, modify, delete, refactor, or "improve" application code on repository import.**

Before taking any implementation action, read:

1. `Main Prompt/Main-Prompt.md`
2. `agents.md`
3. `context/project-overview.md`
4. `context/architecture-context.md`
5. `context/code-standards.md`
6. `context/ai-workflow-rules.md`
7. `context/ui-context.md`
8. `context/progress-tracker.md`

Then determine the **current authorized phase and feature** from the progress tracker.

Only after that may you locate and read the authoritative implementation prompt under the current phase's `ChatGPT Prompt/` directory.

## HARD RULE

Do not treat repository import as a request to build an application.

Do not create a React/Vite/Node/Firebase application.

Do not create demo data, placeholder business logic, mock completion evidence, or invented integrations.

Do not skip to a later phase.

Do not infer missing requirements.

If an authoritative implementation prompt is missing or the next action cannot be determined from the repository, **STOP and report exactly what is missing.**

## ARCHITECTURE

The approved architecture is:

**Google Sites + Google Sheets + Google Forms + Google Drive + Google Apps Script**

Do not replace it with another architecture unless an explicit approved architecture change exists in the repository.

## EXECUTION ENTRY

`Main Prompt/Main-Prompt.md` is the master execution controller.

The required operating sequence is:

**READ → DETERMINE CURRENT STATE → SELECT AUTHORITATIVE PROMPT → UNDERSTAND → PLAN → IMPLEMENT EXACTLY → VERIFY → DOCUMENT → STOP AT BOUNDARY**

This file is intentionally short. Do not duplicate or reinterpret the project's detailed rules here.
