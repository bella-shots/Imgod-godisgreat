# Master Company Repository Agent Entry

This is the root agent instruction entry point for AI coding agents.

## STOP BEFORE BUILDING

This repository is a controlled, specification-first project.

On import, **do not initialize, scaffold, generate, modify, delete, refactor, or improve application code**.

Read `.agents/AGENTS.md` first, then follow its required reading order.

The master execution controller is:

`Main Prompt/Main-Prompt.md`

Do not treat repository import as a request to build an application.

Approved architecture:

**Google Sites + Google Sheets + Google Forms + Google Drive + Google Apps Script**

Never substitute React/Vite/Firebase/another application architecture unless an explicit approved architecture change exists in the repository.

If the current phase, feature, or authoritative implementation prompt cannot be determined, **STOP and request the missing instruction.**
